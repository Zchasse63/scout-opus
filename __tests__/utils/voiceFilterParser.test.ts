describe('voiceFilterParser', () => {
  test('voiceFilterParser module exists', () => {
    const voiceFilterParser = require('../../utils/voiceFilterParser').default;
    expect(voiceFilterParser).toBeDefined();
  });

  test('parseVoiceFilters function exists', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(typeof parseVoiceFilters).toBe('function');
  });

  test('parses gym type filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('parses location filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('parses price filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('parses amenity filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('parses rating filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('parses multiple filters', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles empty input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles null input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles undefined input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles invalid input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('case insensitive parsing', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles typos in input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('returns correct filter object', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles complex voice queries', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('extracts intent from query', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles distance filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles open now filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles budget filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles equipment filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles class filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles trainer filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles group size filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles time filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles date filter', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles combined filters', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('returns normalized output', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles special characters', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles numbers in input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles unicode characters', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles very long input', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('handles whitespace variations', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });

  test('renders without crashing', () => {
    const { parseVoiceFilters } = require('../../utils/voiceFilterParser').default;
    expect(parseVoiceFilters).toBeDefined();
  });
});

